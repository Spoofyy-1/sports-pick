# Copy this to `.env` and fill in your keys. Never commit `.env`.

# Moonshot (Kimi) API key — get one at https://platform.moonshot.ai
# Without this, the /analyze endpoint returns a stub.
MOONSHOT_API_KEY=

# Optional: override the Kimi model
# MOONSHOT_MODEL=kimi-k2-0905-preview
